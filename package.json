{
  "name": "tide-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "example:dev": "pnpm --filter react-app dev",
    "example:generate": "pnpm --filter react-app generate"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "turbo": "^2.3.0",
    "typescript": "^5.7.0",
    "tsup": "^8.3.5",
    "vitest": "^2.1.8"
  },
  "packageManager": "pnpm@9.1.2",
  "engines": {
    "node": ">=20"
  }
}
